# word-boosting

uoost_phrases and uoost_weight params — single phrase, multi pipe separated, case insensitive, edge cases

## Tests

7 tests: single phrase, multi-phrase, case-insensitive, default weight, higher weight, weight w/o phrases, malformed separator

## Endpoints

