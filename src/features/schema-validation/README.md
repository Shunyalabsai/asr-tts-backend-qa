# schema-validation

Response schema validation — segments ordering, words[] fields, audio_duration, inference_time, content type checks

## Tests

8 tests: segments ascending, words fields, audio_duration, inference_time, request_id, no extra fields, Content-Type, Hindi structure

## Endpoints

