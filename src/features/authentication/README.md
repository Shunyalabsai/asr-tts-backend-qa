# authentication

API key → JWT token flow, token caching, expired/invalid key handling

## Tests

8 tests: valid key, missing header, invalid key, empty Bearer, revoked key, case sensitivity, health without auth, key not echoed

## Endpoints

