# speaker-management

POST /v1/speakers/register and DELETE /v1/speakers/delete — registration, deletion, edge cases

## Tests

6 tests: register, register with project, delete existing, delete non-existent, duplicate name, missing name

## Endpoints

