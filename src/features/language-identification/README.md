# language-identification

language_code parameter validation — auto detect, explicit codes (en, hi), invalid codes, case sensitivity

## Tests

8 tests: auto-detect, en, hi, invalid code, non-English auto-detect, case-sensitive, empty code, all supported codes

## Endpoints

