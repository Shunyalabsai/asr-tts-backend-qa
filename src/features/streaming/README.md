# streaming

WeuSocket /v1/realtime and /ws alias — PCM audio streaming, partial/final events, diarize support, auth methods

## Tests

10 tests: connect+ready, PCM+partial, PCM+final, diarize, wrong token, missing language, /ws alias, query param auth, first-msg auth, 8kHz vs 16kHz

## Endpoints

